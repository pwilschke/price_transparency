.Rproj.user
*.Rhistory
.RData
.Ruserdata
# raw downloaded MRF content -- large, regenerable from manifest + urls
data/blobs/

# processed long-format dataset -- large, regenerable from blobs + manifest
data/parquet/

# scratch space
data/tmp/

# curl ETag cache -- regenerable
data/etags/
